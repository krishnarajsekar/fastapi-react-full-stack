# =============================================================================
# MOBILE ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and customize:
#   cp .env.example .env
#
# Expo uses EXPO_PUBLIC_ prefix for environment variables
# These are embedded at build time
#
# IMPORTANT FOR MOBILE:
# Mobile devices CANNOT access localhost (it refers to the device itself)
# You need either:
#   a) Your computer's local IP (e.g., 192.168.1.100) - works on same WiFi
#   b) A tunnel like ngrok - works anywhere, even over cellular
#
# NGROK SETUP:
# 1. Install ngrok: https://ngrok.com/download
# 2. Run: ngrok http 8000
# 3. Copy the https URL below
# =============================================================================

# Option A: Local IP (same WiFi only)
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.100:8000

# Option B: ngrok tunnel (works anywhere)
EXPO_PUBLIC_API_BASE_URL=
